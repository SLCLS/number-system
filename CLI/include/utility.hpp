#ifndef UTILITY_HPP
#define UTILITY_HPP

#include <string>
using std::string;

namespace utility
{
    // Utility for Calculator (Prompt Helper)
    void calculator_help();
    int get_base_selection(string prompt);
}

#endif